/** @type {import('next').NextConfig} */
const nextConfig = {
  // Enable static export for Vercel edge (optional)
  // output: 'export',

  // Add any image domains you'll use for real project photos
  images: {
    remotePatterns: [],
  },
};

module.exports = nextConfig;
